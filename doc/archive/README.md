# Archive

这里保存历史研究记录、过期计划和已经降级的实验方向。

归档文件仍然可能包含有用证据，但不代表当前主线。当前文档入口是：

```text
doc/mtp_latent_reasoning_experiment_design.md
doc/code_experiment_guide.md
doc/transition_representation_analysis_plan.md
```

当前研究判断：

```text
先验证 MTP / codec latent 的 representation usefulness，
再决定是否继续把 transition / continuous rollout 作为主线。
```

其中 `experiment_architecture_overview.html` 是旧版 transition-first 可视化概览，仅作为历史参考。
